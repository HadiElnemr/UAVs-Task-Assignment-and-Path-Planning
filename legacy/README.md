# Legacy code (archived)

Original course-project layout: one folder per algorithm with duplicated `classes.py`, `Test_cases.py`, and plotting modules.

**Use the new entry points instead:** see the repository [README](../README.md) and `runs/`.

Note: the top-level folder named `ACO/` implemented **simulated annealing**, not ant colony optimisation. Ant colony task assignment lives in `mutapp/task_assignment.py` as `ACO_TaskAssignment`.
